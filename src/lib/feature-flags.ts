/**
 * Feature flags for Pulse LAN-Haus.
 *
 * BOOKING_ENABLED — set to `true` once the location is ready to accept
 * reservations. Flipping it back on will restore all booking CTAs and remove
 * the "coming soon" notices site-wide.
 */
export const BOOKING_ENABLED = false;
